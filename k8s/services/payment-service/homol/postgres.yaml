apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: payment-postgres-homol-data
  labels:
    app: payment-postgres-homol
    environment: homol
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-postgres-homol
  labels:
    app: payment-postgres-homol
    environment: homol
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-postgres-homol
  template:
    metadata:
      labels:
        app: payment-postgres-homol
        environment: homol
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: payment-service-homol-secret
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB"
            initialDelaySeconds: 20
            periodSeconds: 20
          volumeMounts:
            - name: payment-postgres-homol-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: payment-postgres-homol-data
          persistentVolumeClaim:
            claimName: payment-postgres-homol-data
---
apiVersion: v1
kind: Service
metadata:
  name: payment-postgres-homol
  labels:
    app: payment-postgres-homol
    environment: homol
spec:
  type: ClusterIP
  selector:
    app: payment-postgres-homol
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
