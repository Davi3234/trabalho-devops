apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: inventory-redis-homol-data
  labels:
    app: inventory-redis-homol
    environment: homol
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 512Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-redis-homol
  labels:
    app: inventory-redis-homol
    environment: homol
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-redis-homol
  template:
    metadata:
      labels:
        app: inventory-redis-homol
        environment: homol
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          env:
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: inventory-service-homol-secret
                  key: REDIS_PASSWORD
          command:
            - sh
            - -c
            - exec redis-server --requirepass "$REDIS_PASSWORD"
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli -a "$REDIS_PASSWORD" ping | grep -q PONG
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - redis-cli -a "$REDIS_PASSWORD" ping | grep -q PONG
            initialDelaySeconds: 20
            periodSeconds: 20
          volumeMounts:
            - name: inventory-redis-homol-data
              mountPath: /data
      volumes:
        - name: inventory-redis-homol-data
          persistentVolumeClaim:
            claimName: inventory-redis-homol-data
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-redis-homol
  labels:
    app: inventory-redis-homol
    environment: homol
spec:
  type: ClusterIP
  selector:
    app: inventory-redis-homol
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
